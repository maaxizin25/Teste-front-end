import styled from "styled-components";

export const CardRedStyled = styled.div`
  padding: 50px;
  background-color: var(--color-primary);
  display: flex;
  flex-direction: column;
  gap: 10px;
  color: var(--color-white);
  span {
    display: flex;
    align-items: center;
    gap: 10px;
    a {
    }
  }
`;
